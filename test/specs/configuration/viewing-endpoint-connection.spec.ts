import { it, describe } from "../../drivers/vitest/driver";

describe("FEATURE: Endpoint connection", () => {
  describe("Rule: Examples should match the current configuration", () => {
    it.todo("Example: The 'Endpoint Configuration Only' tab should be selected by default");
    it.todo("Example: The 'Endpoint Configuration Only' tab should display endpoint configuration examples for the current configuration");
    /* SCENARIO
        Endpoint Configuration only
      */

    it.todo("Example: The 'JSON File' tab should display JSON file configuration examples for the current configuration");
    /* SCENARIO
        Json file
      */
  });
  describe("Rule: Copying the example should happen with a single click", () => {
    it.todo("Example: Clicking the 'Copy' button in the 'Endpoint Configuration Only' tab should copy the example to the clipboard");

    /* SCENARIO
        Endpoint Configuration only
      */

    it.todo("Example: Clicking the 'Copy' button in the 'JSON File' tab should copy the example to the clipboard");
    /* SCENARIO
        Json file
      */
  });
});
