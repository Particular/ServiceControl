<UserControl x:Class="ServiceControl.Config.UI.Shell.BetaTag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="300" d:DesignWidth="300">
    <Viewbox Stretch="Uniform" Width="{Binding ElementName=Root, Path=Width}" Height="{Binding ElementName=Root, Path=Height}">
        <Border Background="#fed21e" Margin="2">
            <TextBlock Text="BETA" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Margin="4"/>
        </Border>
    </Viewbox>
</UserControl>
